'use strict';

describe('myApp.collection module', function() {

  beforeEach(module('myApp.collection'));

  describe('collection controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var collectionCtrl = $controller('CollectionController');
      expect(collectionCtrl).toBeDefined();
    }));

  });
});